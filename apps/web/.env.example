# Variables de entorno para apps/web (Next.js)
# Copiá este archivo a .env y ajustá si hace falta.
# NUNCA commitees el archivo .env con valores reales.

# URL base del API NestJS (apps/api). El BFF del web la usa server-side.
NEXT_PUBLIC_API_URL=http://localhost:3001
